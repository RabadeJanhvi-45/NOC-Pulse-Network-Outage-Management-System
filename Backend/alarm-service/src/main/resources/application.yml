server:
  port: 8083

spring:
  application:
    name: alarm-service
  datasource:
    url: jdbc:h2:file:./data/alarmdb;AUTO_SERVER=TRUE;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
  h2:
    console:
      enabled: true
      path: /h2-console

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

logging:
  level:
    com.noc.alarmservice: INFO

# Set to false to skip the Feign call to device-service on alarm creation
# (useful for running alarm-service standalone / in tests without device-service up).
alarm:
  device-validation:
    enabled: true

# Must match auth-service's jwt.secret / jwt.issuer exactly — this service
# only validates tokens (HS256, shared secret), it never issues them.
jwt:
  secret: dev-only-change-me-network-outage-dashboard-auth-service-secret-key-2024
  issuer: auth-service
